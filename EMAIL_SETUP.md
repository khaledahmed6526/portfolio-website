# إعداد خدمة الإيميلات 📧

هذا الدليل يشرح كيفية إعداد إرسال الإيميلات التلقائية عند استلام رسائل من نموذج الاتصال.

## 📋 ما تم إضافته

✅ نظام إرسال إيميل تلقائي عند استلام رسالة جديدة من Contact Form  
✅ إرسال الرسالة مباشرة إلى بريدك: **akhaledahmedmahamed@gmail.com**  
✅ إرسال رسالة ترحيب تلقائية للعميل (اختياري)  
✅ تنسيق HTML احترافي للإيميلات  

## 🔧 خطوات الإعداد

### الخيار 1: استخدام Gmail (الأسهل)

#### 1. تفعيل App Password في Gmail

لأن Gmail لديه حماية أمنية، يجب إنشاء App Password بدلاً من استخدام كلمة المرور العادية:

1. افتح [Google Account Security](https://myaccount.google.com/security)
2. فعّل **2-Step Verification** إذا لم يكن مفعلاً
3. بعد التفعيل، اذهب إلى **App Passwords**
4. اختر **Mail** ثم **Other (Custom name)**
5. اكتب: "Portfolio Website"
6. انسخ الـ 16-digit password الذي يظهر

#### 2. تحديث ملف .env

أضف هذه الأسطر في ملف `.env`:

```env
# Email Configuration
EMAIL_USER=akhaledahmedmahamed@gmail.com
EMAIL_PASS=xxxx xxxx xxxx xxxx
```

⚠️ **مهم**: استبدل `xxxx xxxx xxxx xxxx` بالـ App Password الذي نسخته

### الخيار 2: استخدام خدمة SMTP أخرى

إذا كنت تريد استخدام خدمة بريد إلكتروني أخرى:

#### Outlook/Hotmail:
```env
EMAIL_SERVICE=hotmail
EMAIL_USER=your-email@outlook.com
EMAIL_PASS=your-password
```

#### Yahoo:
```env
EMAIL_SERVICE=yahoo
EMAIL_USER=your-email@yahoo.com
EMAIL_PASS=your-app-password
```

#### Custom SMTP:
```env
EMAIL_HOST=smtp.your-provider.com
EMAIL_PORT=587
EMAIL_USER=your-email@domain.com
EMAIL_PASS=your-password
```

### الخيار 3: استخدام خدمات احترافية (للإنتاج)

للمواقع الكبيرة، يُفضل استخدام خدمات متخصصة:

#### SendGrid (مجاني حتى 100 إيميل/يوم):
```env
SENDGRID_API_KEY=your-api-key
```

#### Mailgun (مجاني حتى 5000 إيميل/شهر):
```env
MAILGUN_API_KEY=your-api-key
MAILGUN_DOMAIN=your-domain
```

## 🧪 اختبار الإيميلات

بعد الإعداد:

1. شغل السيرفر: `npm run dev`
2. افتح الموقع: http://localhost:3000/contact
3. املأ نموذج الاتصال وأرسله
4. تحقق من بريدك الإلكتروني

## 📝 الملفات المحدثة

1. **server/utils/emailService.js** - خدمة إرسال الإيميلات
2. **server/controllers/messageController.js** - تم تحديثه لإرسال الإيميلات
3. **package.json** - تم إضافة nodemailer

## ⚙️ تخصيص الإيميلات

### تعديل محتوى الإيميل

افتح ملف `server/utils/emailService.js` وعدل في:

```javascript
mailOptions: {
  subject: 'عنوان الإيميل هنا',
  html: `
    <!-- محتوى HTML هنا -->
  `
}
```

### إيقاف رسالة الترحيب للعميل

في `server/controllers/messageController.js`، احذف أو علّق على هذا الجزء:

```javascript
// sendWelcomeEmail({
//   name: message.name,
//   email: message.email
// })
```

## 🔒 الأمان

⚠️ **تحذيرات أمنية مهمة:**

1. ✅ **لا تشارك** ملف `.env` أبداً
2. ✅ **لا ترفع** `.env` على GitHub
3. ✅ استخدم App Passwords بدلاً من كلمة المرور الأساسية
4. ✅ تأكد أن `.env` موجود في `.gitignore`

## 🐛 حل المشاكل الشائعة

### مشكلة: "Invalid login"

**الحل:**
- تأكد من تفعيل 2-Step Verification في Gmail
- استخدم App Password وليس كلمة المرور العادية
- تأكد من عدم وجود مسافات في `.env`

### مشكلة: "Connection timeout"

**الحل:**
- تحقق من اتصال الإنترنت
- تأكد من أن الـ firewall لا يمنع SMTP
- جرب port 465 بدلاً من 587

### مشكلة: الإيميل يصل إلى Spam

**الحل:**
- استخدم خدمات احترافية مثل SendGrid
- أضف SPF و DKIM records
- استخدم domain مخصص للإيميلات

## 📦 تثبيت nodemailer

إذا لم يكن مثبتاً:

```bash
npm install nodemailer
```

## 🧪 اختبار الإعداد

يمكنك اختبار إرسال الإيميل بدون الموقع:

```javascript
// test-email.js
const nodemailer = require('nodemailer');

const transporter = nodemailer.createTransporter({
  service: 'gmail',
  auth: {
    user: 'akhaledahmedmahamed@gmail.com',
    pass: 'your-app-password'
  }
});

transporter.sendMail({
  from: 'akhaledahmedmahamed@gmail.com',
  to: 'akhaledahmedmahamed@gmail.com',
  subject: 'Test Email',
  text: 'تجربة إرسال إيميل'
}, (err, info) => {
  if (err) console.log('Error:', err);
  else console.log('Success:', info);
});
```

شغله: `node test-email.js`

## 💡 نصائح إضافية

1. **للتطوير**: استخدم [Mailtrap.io](https://mailtrap.io/) لاختبار الإيميلات بدون إرسال حقيقي
2. **للإنتاج**: استخدم خدمات احترافية مثل SendGrid أو AWS SES
3. **الأمان**: قم بتغيير App Password بشكل دوري
4. **المراقبة**: راقب عدد الإيميلات المرسلة لتجنب تجاوز الحدود

## 📞 الدعم

إذا واجهت أي مشكلة:
- راجع console logs في السيرفر
- تحقق من ملف `.env`
- تأكد من صحة بيانات Gmail

---

**✅ بعد الإعداد، ستصلك رسالة على بريدك فوراً عند أي شخص يملأ نموذج الاتصال!**
